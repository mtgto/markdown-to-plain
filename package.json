{
  "name": "markdown-to-plain",
  "version": "1.0.1",
  "description": "An Chrome extension which convert markdown to plain text.",
  "scripts": {
    "build": "npm run build:dev",
    "build:dev": "webpack --mode development",
    "build:prod": "webpack --mode production",
    "build:license": "yarn licenses generate-disclaimer --silent --production --non-interactive --emoji true > src/res/ACKNOWLEDGEMENT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/mtgto/markdwon-to-plain",
  "author": "mtgto <hogerappa@gmail.com>",
  "license": "Apache2",
  "private": true,
  "dependencies": {
    "@anydown/maildown": "^1.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.86",
    "copy-webpack-plugin": "^5.0.3",
    "ts-loader": "^6.0.4",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3",
    "webpack": "^4.35.3",
    "webpack-command": "^0.4.2"
  }
}
